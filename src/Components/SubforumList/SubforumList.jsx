import React, { useContext } from 'react';
// import ContextProvider from '../Context/ContextProvider';
import Subforum from '../Subforum/Subforum';
import Context from '../Context/Context';

const SubforumList = () => {
    // const { subforums } = useContext(Context); // Access subforums from context

    // return (
    //     <div className='SubforumList'>
    //         <h2>Forum Subforums</h2>
    //         {subforums.map((subforum) => ( // Iterate over subforums array
    //             <Subforum key={subforum.title} subforum={subforum} />
    //         ))}
    //     </div>
    // );
};

export default SubforumList;
